import { useEffect, useMemo, useState } from 'react'
import { ArrowRight, BatteryCharging, Check, ChevronDown, Factory, Menu, Phone, ShieldCheck, Sun, Wrench, X, Zap } from 'lucide-react'
import { company, generatorItems } from './data/config'
import { generatorConfig } from './data/generatorConfig'
import { solarConfig } from './data/solarConfig'
import { detailedFaqs } from './data/faqs'
import { panelTypes, services, stabilizerTypes } from './data/services'

type QuoteType = 'solar' | 'generator'
type Load = { quantity: number; running: number; surge: number }

const money = (value: number | null) => value === null ? 'Not configured' : `₹${Math.round(value).toLocaleString('en-IN')}`
const imageSolar = 'https://images.unsplash.com/photo-1509391366360-2e959784a276?auto=format&fit=crop&w=1400&q=85'
const imageGenerator = 'https://images.unsplash.com/photo-1581092160607-ee22621dd758?auto=format&fit=crop&w=1200&q=85'
const powerSlides = [
  { src: 'https://images.unsplash.com/photo-1509391366360-2e959784a276?auto=format&fit=crop&w=1400&q=85', alt: 'Solar panels installed on a commercial rooftop', label: 'Solar energy' },
  { src: 'https://images.unsplash.com/photo-1581092160607-ee22621dd758?auto=format&fit=crop&w=1400&q=85', alt: 'Industrial power equipment and generator service', label: 'Generator solutions' },
  { src: 'https://images.unsplash.com/photo-1497435334941-8c899ee9e8e9?auto=format&fit=crop&w=1400&q=85', alt: 'Large solar installation in an energy facility', label: 'Solar projects' },
  { src: 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?auto=format&fit=crop&w=1400&q=85', alt: 'Technician working with electrical equipment', label: 'Service & support' },
]

function App() {
  const [mobileOpen, setMobileOpen] = useState(false)
  const [solarBill, setSolarBill] = useState(5000)
  const [billInput, setBillInput] = useState('5000')
  const [solarType, setSolarType] = useState('Commercial')
  const [roofArea, setRoofArea] = useState(800)
  const [quoteType, setQuoteType] = useState<QuoteType>('solar')
  const [submitted, setSubmitted] = useState(false)
  const [faqOpen, setFaqOpen] = useState(0)
  const [finderChoice, setFinderChoice] = useState('')
  const [slideIndex, setSlideIndex] = useState(0)
  const [loads, setLoads] = useState<Record<string, Load>>(
    Object.fromEntries(generatorItems.map((item) => [item.name, { quantity: 0, running: item.running, surge: item.surge }])),
  )

  const solar = useMemo(() => {
    const generationFactor = solarConfig.propertyGenerationFactor[solarType as keyof typeof solarConfig.propertyGenerationFactor]
    const tariff = solarType === 'Commercial' ? solarConfig.tariff.commercial.energyRate : solarType === 'Industrial' ? solarConfig.tariff.industrial.energyRate : solarConfig.tariff.residential.energyRate
    const generationPerKwYear = solarConfig.generation.typical
    const byBill = tariff ? (solarBill * 12) / (tariff * generationPerKwYear * solarConfig.selfConsumption.typical * generationFactor) : 0
    const byRoof = (roofArea / 100) * 0.55
    const capacity = Math.max(1, Math.min(100, Math.min(byBill || byRoof, byRoof || 100)))
    const annualGridCost = solarBill * 12
    const annualGeneration = capacity * generationPerKwYear * generationFactor
    const annualSavings = tariff ? Math.min(annualGeneration * tariff * solarConfig.selfConsumption.typical, annualGridCost) : null
    const annualGridCostAfterSolar = annualSavings === null ? annualGridCost : Math.max(0, annualGridCost - annualSavings)
    return {
      capacity: Math.round(capacity * 10) / 10,
      generation: Math.round(annualGeneration),
      monthlySavings: annualSavings === null ? null : Math.round(annualSavings / 12),
      annualSavings: annualSavings === null ? null : Math.round(annualSavings),
      cost: solarConfig.projectCost.typical,
      payback: solarConfig.projectCost.typical && annualSavings ? Math.max(1, solarConfig.projectCost.typical * capacity / annualSavings).toFixed(1) : 'Not configured',
      co2: (annualGeneration * solarConfig.emissions.factor / 1000).toFixed(1),
      panels: solarConfig.panel.wattage ? Math.ceil((capacity * 1000) / solarConfig.panel.wattage) : 'Not configured',
      billCapacity: tariff ? Math.round(byBill * 10) / 10 : 'Not configured',
      roofCapacity: Math.round(byRoof * 10) / 10,
      isRoofLimited: Boolean(tariff && byRoof > 0 && byRoof < byBill),
      tariff,
      annualGridCost,
      annualGridCostAfterSolar,
    }
  }, [solarBill, roofArea, solarType])

  const generator = useMemo(() => {
    const running = Object.values(loads).reduce((sum, load) => sum + load.quantity * load.running, 0)
    const peak = Object.values(loads).reduce((sum, load) => sum + load.quantity * load.surge, 0)
    const kva = Math.ceil(Math.max(running, peak) * generatorConfig.safetyMargin / 1000)
    const recommended = kva <= generatorConfig.minimumRecommendationKva ? generatorConfig.minimumRecommendationKva : kva <= 7.5 ? 7.5 : kva <= 10 ? 10 : Math.ceil(kva / 5) * 5
    return { running: (running / 1000).toFixed(1), peak: (peak / 1000).toFixed(1), recommended }
  }, [loads])

  const solarComparisonCost = solar.annualGridCostAfterSolar ?? solar.annualGridCost

  const updateLoad = (name: string, quantity: number) => setLoads((current) => ({ ...current, [name]: { ...current[name], quantity: Math.max(0, quantity) } }))

  const scrollToQuote = (type: QuoteType) => {
    window.location.href = '/request-quote'
  }

  const currentPath = window.location.pathname

  useEffect(() => {
    const timer = window.setInterval(() => setSlideIndex((index) => (index + 1) % powerSlides.length), 3000)
    return () => window.clearInterval(timer)
  }, [])

  if (currentPath === '/request-quote' || currentPath === '/contact') {
    return <RequestQuotePage />
  }

  if (currentPath !== '/') {
    return <InnerPage path={currentPath} />
  }

  return (
    <div className="app-shell">
      <header className="topbar">
        <div className="container nav-row">
          <a href="#top" className="brand" onClick={() => setMobileOpen(false)}>
            <span className="brand-mark"><Zap size={18} fill="currentColor" /></span>
            <span><strong>shriamman</strong><small>ENTERPRISES</small></span>
          </a>
          <nav className={mobileOpen ? 'main-nav open' : 'main-nav'}>
            <a href="/solar-solutions" onClick={() => setMobileOpen(false)}>Solar</a><a href="/generator-solutions" onClick={() => setMobileOpen(false)}>Generators</a><a href="/electrical-panels" onClick={() => setMobileOpen(false)}>Panels</a><a href="/stabilizers" onClick={() => setMobileOpen(false)}>Stabilizers</a><a href="/services" onClick={() => setMobileOpen(false)}>Services</a><a href="/projects" onClick={() => setMobileOpen(false)}>Projects</a><a href="/about" onClick={() => setMobileOpen(false)}>About</a><a href="/contact" onClick={() => setMobileOpen(false)}>Contact</a>
          </nav>
          <div className="nav-actions"><a className="phone-link" href={`tel:${company.phone}`}><Phone size={15} /> {company.phone}</a><a className="button button-dark button-small" href="/request-quote">Get a quote <ArrowRight size={15} /></a><button className="menu-button" aria-label="Toggle menu" onClick={() => setMobileOpen((open) => !open)}>{mobileOpen ? <X /> : <Menu />}</button></div>
        </div>
      </header>

      <main id="top">
        <section className="hero container">
          <div className="hero-copy reveal"><p className="kicker">POWER &amp; ENERGY SOLUTIONS · EST. 2005</p><h1>Reliable Power.<br /><em>Smarter Energy.</em></h1><p className="hero-lead">From dependable backup power to solar energy solutions, Shri Amman Enterprises provides practical power solutions designed around your requirements.</p><div className="hero-actions"><a className="button button-accent" href="#solutions">Explore solutions <ArrowRight size={17} /></a><button className="button button-outline" onClick={() => scrollToQuote('generator')}>Get a quote</button></div><div className="trust-strip"><span><Check size={16} /> Established 2005</span><span><Check size={16} /> 21+ years experience</span><span><Check size={16} /> Service &amp; support</span></div></div>
          <div className="hero-visual reveal"><img src={powerSlides[slideIndex].src} alt={powerSlides[slideIndex].alt} /><div className="hero-float"><span className="status-dot" /><div><span>Complete power solutions · {powerSlides[slideIndex].label}</span><strong>Solar + backup</strong><small>Designed around your requirement</small></div></div><div className="hero-stamp">21<br /><small>YEARS</small></div><div className="slide-controls" aria-label="Power solution images">{powerSlides.map((slide, index) => <button key={slide.src} className={index === slideIndex ? 'active' : ''} aria-label={`Show ${slide.label} image`} onClick={() => setSlideIndex(index)} />)}</div></div>
        </section>

        <section className="solutions-section" id="solutions"><div className="container"><div className="section-heading"><p className="kicker">TWO WAYS TO STAY POWERED</p><h2>Choose your power solution.</h2><p>Start with the business problem you want to solve. We will help you find the right mix of equipment, installation and support.</p></div><div className="vertical-grid"><article className="vertical-card solar-card"><div><span className="round-icon"><Sun size={24} /></span><p className="card-label">01 / CLEAN ENERGY</p><h3>Solar energy<br />solutions</h3><p>Generate your own clean power and reduce long-term electricity costs with a system designed around your consumption.</p><button onClick={() => document.getElementById('solar-tool')?.scrollIntoView({ behavior: 'smooth' })}>Estimate solar potential <ArrowRight size={17} /></button></div><img src={imageSolar} alt="Rooftop solar installation" /></article><article className="vertical-card generator-card"><div><span className="round-icon"><BatteryCharging size={24} /></span><p className="card-label">02 / POWER CONTINUITY</p><h3>Generator &amp;<br />power solutions</h3><p>Reliable backup power for homes, businesses and critical operations, backed by responsive service.</p><button onClick={() => document.getElementById('generator-tool')?.scrollIntoView({ behavior: 'smooth' })}>Size a generator <ArrowRight size={17} /></button></div><img src={imageGenerator} alt="Industrial power equipment" /></article></div></div></section>

        <section className="solution-matrix"><div className="container"><div className="section-heading"><p className="kicker">FOUR WAYS TO GET THE RIGHT SUPPORT</p><h2>What power solution do you need?</h2><p>Whether you need to reduce electricity costs, protect your business from interruptions, or improve your electrical infrastructure, start with the requirement.</p></div><div className="matrix-grid"><MatrixCard icon={<Sun />} number="01" title="Generate. Save. Sustain." text="Solar energy solutions designed around consumption, available space and application requirements." href="/solar-solutions" /><MatrixCard icon={<BatteryCharging />} number="02" title="Power when you need it." text="Generator solutions for backup power across commercial, industrial and other applications." href="/generator-solutions" /><MatrixCard icon={<Factory />} number="03" title="Built around your power requirements." text="Electrical panels for organized, safe and reliable power distribution." href="/electrical-panels" /><MatrixCard icon={<ShieldCheck />} number="04" title="Stable voltage. Protected equipment." text="Stabilizer solutions designed to help protect electrical and electronic equipment." href="/stabilizers" /></div></div></section>

        <section className="tool-section container" id="solar-tool"><div className="tool-intro"><p className="kicker">INTERACTIVE ESTIMATE</p><h2>Estimate your solar potential.</h2><p>A quick first look at capacity, savings and payback. Your actual system will depend on site conditions, equipment and tariff.</p><div className="tool-note"><Sun size={18} /><span>Adjust the bill, property and roof area. Every result updates immediately.</span></div></div><div className="calculator solar-calculator"><div className="field-group"><label htmlFor="bill-manual">Average monthly electricity bill</label><div className="bill-input-row"><div className="range-value">{money(solarBill)}</div><label className="manual-bill-label" htmlFor="bill-manual">Enter amount<input id="bill-manual" type="number" min="500" max="30000" step="500" value={billInput} onChange={(event) => { setBillInput(event.target.value); const value = Math.min(30000, Math.max(500, Number(event.target.value) || 500)); setSolarBill(value) }} /></label></div><input id="bill" aria-label="Monthly bill slider" type="range" min="500" max="30000" step="500" value={solarBill} onChange={(event) => { setSolarBill(Number(event.target.value)); setBillInput(event.target.value) }} /><div className="range-labels"><span>₹500</span><span>₹30,000+</span></div></div><div className="field-row"><label>Property type<select value={solarType} onChange={(event) => setSolarType(event.target.value)}><option>Residential</option><option>Commercial</option><option>Industrial</option></select><small>Used in the generation estimate</small></label><label>Available roof area<input type="number" min="100" value={roofArea} onChange={(event) => setRoofArea(Number(event.target.value) || 0)} /><small>Square feet</small></label></div><div className="results-grid"><Result label="Recommended system" value={`${solar.capacity} kW`} /><Result label="Annual generation" value={`${solar.generation.toLocaleString('en-IN')} kWh`} /><Result label="Annual savings" value={money(solar.annualSavings)} /><Result label="Payback estimate" value={`${solar.payback} years`} /></div>{solar.isRoofLimited && <div className="calculator-notice"><ShieldCheck size={17} /><span>Your roof area currently limits the estimate to {solar.roofCapacity} kW. Add more available area above to see the system size rise with a higher bill.</span></div>}<div className="cost-comparison"><div className="comparison-heading"><span>Estimated annual electricity cost</span><strong>{money(solar.annualSavings)} potential saving</strong></div><div className="cost-row"><div><span>Without solar</span><strong>{money(solar.annualGridCost)}</strong></div><i className="bar full" style={{ width: '100%' }} /></div><div className="cost-row"><div><span>With estimated solar</span><strong>{money(solar.annualGridCostAfterSolar)}</strong></div><i className="bar partial" style={{ width: `${Math.max(8, solar.annualGridCostAfterSolar / Math.max(1, solar.annualGridCost) * 100)}%` }} /></div><p>Illustrative first-year comparison based on the inputs above. Actual bills vary with consumption, tariff and generation.</p></div><div className="estimate-details"><span><strong>{solar.billCapacity} kW</strong> bill-based need</span><span><strong>{solar.roofCapacity} kW</strong> roof capacity</span><span><strong>{money(solar.cost)}</strong> project estimate</span><span><strong>{solar.panels}</strong> panels</span><span><strong>{solar.co2} t</strong> CO₂ reduction / year</span></div><button className="button button-dark" onClick={() => scrollToQuote('solar')}>Request a site assessment <ArrowRight size={16} /></button><p className="disclaimer">Calculator results are indicative estimates only and are not a final quotation or engineering recommendation. Actual system size, generation, savings and project cost may vary.</p></div></section>

        <section className="dark-band" id="generator-tool"><div className="container tool-section generator-tool"><div className="tool-intro"><p className="kicker">LOAD CALCULATOR</p><h2>Find the right generator capacity.</h2><p>Select the equipment you need to keep running. We apply a practical starting-load and safety-margin estimate.</p><div className="tool-note"><ShieldCheck size={18} /><span>Professional load assessment is recommended before purchase.</span></div></div><div className="calculator generator-calculator"><div className="load-list">{generatorItems.map((item) => <div className="load-row" key={item.name}><div><strong>{item.name}</strong><small>{item.running}W running · {item.surge}W starting</small></div><input aria-label={`${item.name} quantity`} type="number" min="0" max="99" value={loads[item.name].quantity || ''} placeholder="0" onChange={(event) => updateLoad(item.name, Number(event.target.value))} /></div>)}</div><div className="generator-result"><span>Recommended generator</span><strong>{generator.recommended} kVA</strong><div><span>Running load <b>{generator.running} kW</b></span><span>Peak estimate <b>{generator.peak} kW</b></span></div><button className="button button-accent" onClick={() => scrollToQuote('generator')}>Request generator quote <ArrowRight size={16} /></button></div></div></div></section>

        <section className="finder-band"><div className="container finder-grid"><div><p className="kicker">POWER SOLUTION FINDER</p><h2>What are you trying to achieve?</h2><p>Choose the outcome you care about and we will point you toward the relevant starting place.</p></div><div className="finder-options">{[['Reduce electricity costs', 'Solar Solutions', '/solar-solutions'], ['Protect against power cuts', 'Generator Solutions', '/generator-solutions'], ['Improve electrical distribution', 'Electrical Panels', '/electrical-panels'], ['Protect equipment from fluctuations', 'Stabilizer Solutions', '/stabilizers']].map(([label, result, href]) => <a className={finderChoice === label ? 'finder-option active' : 'finder-option'} href={finderChoice === label ? href : '#finder'} key={label} onClick={(event) => { if (finderChoice !== label) { event.preventDefault(); setFinderChoice(label) } }}><span>{label}</span><strong>{finderChoice === label ? result : 'Choose this'}</strong><ArrowRight size={16} /></a>)}</div></div></section>

        <section className="infrastructure-section container"><div className="section-heading split-heading"><div><p className="kicker">ELECTRICAL INFRASTRUCTURE</p><h2>Panels and stabilizers for dependable distribution.</h2></div><p>Electrical infrastructure should be organized around the application. We keep the descriptions here deliberately practical until confirmed product specifications are added.</p></div><div className="infra-grid"><article className="infra-panel"><span className="round-icon"><Factory size={23} /></span><h3>Designed around your power distribution needs.</h3><p>Panel configurations can be shaped around the project requirement and electrical distribution plan.</p><div className="infra-list">{panelTypes.slice(0, 4).map((item) => <span key={item.title}><Check size={14} /> {item.title}</span>)}</div><a className="text-link" href="/electrical-panels">Explore electrical panels <ArrowRight size={15} /></a></article><article className="infra-panel stabilizer-panel"><span className="round-icon"><ShieldCheck size={23} /></span><h3>Stable voltage. Protected equipment.</h3><p>Stabilizer solutions for applications where voltage control and equipment protection matter.</p><div className="infra-list">{stabilizerTypes.slice(0, 3).map((item) => <span key={item.title}><Check size={14} /> {item.title}</span>)}</div><a className="text-link" href="/stabilizers">Explore stabilizers <ArrowRight size={15} /></a></article></div></section>

        <section className="services container" id="projects"><div className="section-heading split-heading"><div><p className="kicker">WHAT WE HANDLE</p><h2>Power that works<br />for your operation.</h2></div><p>From a single generator service call to a planned solar installation, the work starts with understanding your load, site and priorities.</p></div><div className="service-grid">{services.map((service, index) => <Service key={service.title} icon={[<Sun />, <Zap />, <Wrench />, <ShieldCheck />][index]} title={service.title} text={service.text} />)}</div></section>

        <section className="process-band" id="about"><div className="container"><div className="section-heading"><p className="kicker">A CLEARER WAY TO START</p><h2>From requirement to reliable power.</h2></div><div className="process-grid">{[['01', 'Understand', 'Your load, site, operating hours and business priorities.'], ['02', 'Recommend', 'The right generator, solar system or combination for the job.'], ['03', 'Deliver', 'Procurement, installation, commissioning and handover.'], ['04', 'Support', 'Service, maintenance and practical help after installation.']].map(([number, title, text]) => <div className="process-step" key={number}><span>{number}</span><h3>{title}</h3><p>{text}</p></div>)}</div></div></section>

        <section className="review-section"><div className="container"><div className="section-heading"><p className="kicker">CUSTOMER FEEDBACK</p><h2>Verified experiences belong here.</h2><p>We will publish approved customer feedback once the company supplies the wording and permission to use it. No testimonials are invented in this preview.</p></div><div className="review-grid"><ReviewPlaceholder /><ReviewPlaceholder /><ReviewPlaceholder /></div></div></section>

        <section className="faq-section container" id="contact"><div className="section-heading"><p className="kicker">GOOD QUESTIONS DESERVE CLEAR ANSWERS</p><h2>Before you make a power decision.</h2></div><div className="faq-layout"><div className="faq-list">{detailedFaqs.map(([question, answer], index) => <div className={`faq-item ${faqOpen === index ? 'is-open' : ''}`} key={question}><button aria-expanded={faqOpen === index} onClick={() => setFaqOpen(faqOpen === index ? -1 : index)}><span>{question}</span><ChevronDown size={19} /></button>{faqOpen === index && <p>{answer}</p>}</div>)}</div><div className="quote-panel"><p className="kicker">LET'S TALK POWER</p><h3>Build a solution around your site.</h3><p>Tell us what you are planning and we will come back with the next practical step.</p><div className="quote-tabs"><button className={quoteType === 'solar' ? 'active' : ''} onClick={() => setQuoteType('solar')}><Sun size={17} /> I need solar</button><button className={quoteType === 'generator' ? 'active' : ''} onClick={() => setQuoteType('generator')}><BatteryCharging size={17} /> I need a generator</button></div>{submitted ? <div className="success-message"><Check size={22} /><strong>Request noted for this preview.</strong><span>Connect your real form endpoint to receive leads.</span></div> : <form onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><label>Name<input required placeholder="Your name" /></label><label>Phone<input required placeholder="Phone number" /></label><label>Email<input type="email" placeholder="you@company.com" /></label><label>What do you need?<select value={quoteType} onChange={(event) => setQuoteType(event.target.value as QuoteType)}><option value="solar">Solar solution</option><option value="generator">Generator & power solution</option><option value="panel">Electrical panel</option><option value="stabilizer">Stabilizer</option></select></label><button className="button button-accent" type="submit">Request a free consultation <ArrowRight size={16} /></button></form>}</div></div></section>
      </main>

      <Footer />
      <div className="mobile-cta"><a href={`tel:${company.phone}`}><Phone size={17} /> Call</a><button onClick={() => scrollToQuote('generator')}><Zap size={17} /> Get quote</button></div>
    </div>
  )
}

function RequestQuotePage() {
  const [submitted, setSubmitted] = useState(false)
  return <div className="app-shell"><header className="topbar"><div className="container nav-row"><a href="/" className="brand"><span className="brand-mark"><Zap size={18} fill="currentColor" /></span><span><strong>shriamman</strong><small>ENTERPRISES</small></span></a><nav className="main-nav"><a href="/solar-solutions">Solar</a><a href="/generator-solutions">Generators</a><a href="/electrical-panels">Panels</a><a href="/stabilizers">Stabilizers</a><a href="/services">Services</a><a href="/contact">Contact</a></nav><div className="nav-actions"><a className="button button-dark button-small" href="/">Back to home</a></div></div></header><main><section className="rfq-hero"><div className="container"><p className="kicker">RFQ / ENQUIRY</p><h1>Tell us about your power or solar requirement.</h1><p>Share the details available to you. The more context you provide, the easier it is for our team to understand the next practical step.</p></div></section><section className="rfq-section container"><div className="rfq-intro"><p className="kicker">REQUEST A QUOTE</p><h2>Share the details and our team will respond.</h2><p>Use this enquiry for solar, generators, electrical panels, stabilizers, service or maintenance requirements. Missing information can be discussed later.</p><div className="rfq-company-note"><strong>{company.name}</strong><span>{company.address}</span><span>Established {company.established} · GST IN: {company.gst}</span></div></div>{submitted ? <div className="rfq-success"><Check size={28} /><h2>Thank you for contacting Shri Amman Enterprises.</h2><p>We have received your enquiry in this preview. Connect the form to your email, CRM or WhatsApp endpoint before publishing.</p><a className="button button-dark" href="/">Return to home <ArrowRight size={16} /></a></div> : <form className="rfq-form" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><div className="rfq-grid"><label>Name *<input required name="name" placeholder="Your full name" /></label><label>Company Name *<input required name="company" placeholder="Company or organization" /></label><label>Designation<input name="designation" placeholder="Your role" /></label><label>Email *<input required type="email" name="email" placeholder="you@company.com" /></label><label>Phone *<input required name="phone" placeholder="Phone number" /></label><label>Location *<input required name="location" placeholder="City / project location" /></label><label>Requirement Type *<select required name="requirement"><option value="">Select requirement</option><option>Solar Panels</option><option>Complete Solar System</option><option>Rooftop Solar</option><option>Industrial Solar</option><option>Commercial Solar</option><option>Generator</option><option>Generator Service</option><option>Solar O&amp;M</option><option>Other</option></select></label><label>Project Type<select name="project"><option>New Project</option><option>Expansion</option><option>Replacement</option><option>Procurement Only</option><option>Service</option></select></label><label>Estimated Capacity<select name="capacity"><option>Not decided</option><option>&lt;50 kW</option><option>50–100 kW</option><option>100–500 kW</option><option>500 kW–1 MW</option><option>1 MW+</option><option>Custom</option></select></label><label>Expected Requirement Date<input type="date" name="date" /></label><label className="rfq-wide">Message / Requirement Details<textarea name="message" rows={6} placeholder="Describe the equipment, application, timeline or service requirement"></textarea></label><label className="rfq-wide file-field">Upload Technical Specification / BOQ / RFQ<input type="file" name="attachment" accept=".pdf,.doc,.docx,.xls,.xlsx,.csv,.jpg,.jpeg,.png" /><small>Optional. PDF, document, spreadsheet or image.</small></label></div><div className="rfq-submit"><p>By submitting, you are requesting a conversation. This preview does not send email until an enquiry endpoint is connected.</p><button className="button button-accent" type="submit">Submit enquiry <ArrowRight size={16} /></button></div></form>}</section></main><Footer /></div>
}

function InnerPage({ path }: { path: string }) {
  const [mobileOpen, setMobileOpen] = useState(false)
  const [sent, setSent] = useState(false)
  const onQuote = () => { window.location.href = '/request-quote' }
  const page = path.includes('solar') ? solarPage : path.includes('generator') ? generatorPage : path.includes('panels') ? panelPage : path.includes('stabilizer') ? stabilizerPage : path.includes('services') ? servicesPage : path.includes('projects') ? projectsPage : path.includes('about') ? aboutPage : contactPage
  return <div className="app-shell"><header className="topbar"><div className="container nav-row"><a href="/" className="brand"><span className="brand-mark"><Zap size={18} fill="currentColor" /></span><span><strong>shriamman</strong><small>ENTERPRISES</small></span></a><nav className={mobileOpen ? 'main-nav open' : 'main-nav'}><a href="/solar-solutions" onClick={() => setMobileOpen(false)}>Solar solutions</a><a href="/generator-solutions" onClick={() => setMobileOpen(false)}>Generators</a><a href="/projects" onClick={() => setMobileOpen(false)}>Projects</a><a href="/about" onClick={() => setMobileOpen(false)}>About</a><a href="/contact" onClick={() => setMobileOpen(false)}>Contact</a></nav><div className="nav-actions"><a className="phone-link" href={`tel:${company.phone}`}><Phone size={15} /> {company.phone}</a><button className="button button-dark button-small" onClick={onQuote}>Get a quote <ArrowRight size={15} /></button><button className="menu-button" aria-label="Toggle menu" onClick={() => setMobileOpen((open) => !open)}>{mobileOpen ? <X /> : <Menu />}</button></div></div></header><main><section className="inner-hero"><div className="container"><p className="kicker">{page.kicker}</p><h1>{page.title}</h1><p>{page.intro}</p><button className="button button-accent" onClick={onQuote}>Discuss your requirement <ArrowRight size={16} /></button></div></section><section className="inner-content container"><div className="inner-copy"><p className="kicker">{page.overline}</p><h2>{page.heading}</h2><p>{page.description}</p></div><div className="detail-grid">{page.cards.map((card) => <article className="detail-card" key={card.title}><span className="service-icon">{card.icon}</span><h3>{card.title}</h3><p>{card.text}</p><a href={(card as { href?: string }).href || '/contact'}>Learn more <ArrowRight size={15} /></a></article>)}</div>{path === '/contact' && <div className="inner-quote" id="quote"><div><p className="kicker">REQUEST A CONSULTATION</p><h2>Tell us what you need.</h2><p>Share a few details and connect this preview form to your preferred email or CRM before launch.</p></div>{sent ? <div className="success-message"><Check size={22} /><strong>Request noted for this preview.</strong><span>Connect your real form endpoint to receive leads.</span></div> : <form onSubmit={(event) => { event.preventDefault(); setSent(true) }}><label>Name<input required placeholder="Your name" /></label><label>Phone<input required placeholder="Phone number" /></label><label>Email<input type="email" placeholder="you@company.com" /></label><label>Requirement<select><option>Solar solution</option><option>Generator solution</option><option>Electrical panels or stabilizer</option><option>Service or AMC</option></select></label><button className="button button-accent" type="submit">Send requirement <ArrowRight size={16} /></button></form>}</div>}</section><section className="inner-band"><div className="container inner-band-grid"><div><p className="kicker">WHY WORK WITH US</p><h2>{page.bandTitle}</h2></div><p>{page.bandText}</p></div></section></main><Footer /></div>
}

const solarPage = { kicker: 'SOLAR ENERGY SOLUTIONS', title: 'Clean power, planned around your site.', intro: 'Rooftop and commercial solar guidance that begins with your consumption, roof area and long-term energy goals.', overline: 'FROM FIRST ESTIMATE TO HANDOVER', heading: 'A practical solar journey for homes, businesses and industries.', description: 'We help you understand the opportunity before recommending equipment. The final system design should follow a site assessment, electrical review and a clear proposal.', bandTitle: 'Solar is more than panels on a roof.', bandText: 'The right result comes from matching generation, usage, roof condition, electrical integration and future service support. Use the estimate tool on the homepage as a starting conversation.', cards: [{ title: 'Residential solar', text: 'Explore rooftop solar options for homes and apartment communities looking to reduce grid dependence.', icon: <Sun />, href: '/contact' }, { title: 'Commercial solar', text: 'Plan systems for offices, retail spaces, institutions and commercial buildings with daytime consumption.', icon: <Factory />, href: '/contact' }, { title: 'Industrial solar', text: 'Evaluate larger energy requirements for factories, warehouses and production environments.', icon: <Zap />, href: '/contact' }, { title: 'Solar inspection & support', text: 'Discuss performance checks, maintenance planning and long-term operational support.', icon: <Wrench />, href: '/contact' }] }
const generatorPage = { kicker: 'GENERATOR & POWER SOLUTIONS', title: 'Reliable power when operations cannot stop.', intro: 'Sales, installation and service support for generator requirements across commercial, industrial, residential and critical-use sites.', overline: 'POWER CONTINUITY', heading: 'A generator solution should fit the load, the site and the way you operate.', description: 'We help you move from a rough requirement to a practical generator recommendation, electrical integration plan and support arrangement. Actual selection depends on professional load assessment.', bandTitle: 'Service is part of the solution.', bandText: 'From preventive maintenance and breakdown support to AMC planning, our focus is on keeping the equipment useful long after installation.', cards: [{ title: 'Generator sales', text: 'Discuss the power rating, application, operating pattern and installation environment before selecting equipment.', icon: <BatteryCharging />, href: '/contact' }, { title: 'Installation & commissioning', text: 'Coordinate placement, electrical integration, testing and handover around your site requirements.', icon: <Zap />, href: '/contact' }, { title: 'Electrical panels & stabilizers', text: 'Manufacture and supply electrical panels and stabilizers for dependable power distribution.', icon: <Factory />, href: '/contact' }, { title: 'Service & AMC', text: 'Plan preventive maintenance, breakdown support and ongoing service for your equipment.', icon: <Wrench />, href: '/contact' }] }
const projectsPage = { kicker: 'PROJECTS & APPLICATIONS', title: 'Power solutions for real operating environments.', intro: 'Every site has a different load, schedule and constraint. We use that context to shape the recommendation.', overline: 'WHERE OUR WORK FITS', heading: 'From business premises to industrial operations.', description: 'Project information and client details can be added here as the company builds its public portfolio. Until then, this page explains the types of requirements we support without inventing project claims.', bandTitle: 'Have a project in mind?', bandText: 'Share your location, approximate load and timeline. We can start with a site or load discussion and identify the next practical step.', cards: [{ title: 'Commercial buildings', text: 'Backup power and solar planning for offices, retail, schools, hospitals and institutions.', icon: <Factory />, href: '/contact' }, { title: 'Manufacturing & warehouses', text: 'Power continuity and energy planning for equipment, production areas and storage facilities.', icon: <ShieldCheck />, href: '/contact' }, { title: 'Residential requirements', text: 'Home generator and rooftop solar conversations sized around everyday usage.', icon: <Sun />, href: '/contact' }, { title: 'Critical operations', text: 'Start with a clear load assessment for IT, healthcare, hospitality and other essential applications.', icon: <BatteryCharging />, href: '/contact' }] }
const aboutPage = { kicker: 'ABOUT SHRI AMMAN ENTERPRISES', title: '21 years of practical power experience.', intro: 'Established in 2005, Shri Amman Enterprises serves Chennai with generator sales and service, electrical panels, stabilizers and growing solar capabilities.', overline: 'OUR FOUNDATION', heading: 'A service-led electrical and power solutions company.', description: 'Our work is grounded in understanding the requirement, coordinating the right equipment and supporting it after installation. We do not replace site assessment with generic promises; we build the recommendation around the actual application.', bandTitle: 'Built on useful work.', bandText: 'The company continues to expand from established generator and electrical services into solar energy solutions while keeping practical support at the centre.', cards: [{ title: 'Established in 2005', text: '21 years of operating experience in power and electrical requirements.', icon: <ShieldCheck /> }, { title: 'Chennai based', text: 'Located at No 55/6, P.H Road, Arumbakkam, Chennai - 600106.', icon: <Factory /> }, { title: 'What we provide', text: 'Generator sales and service, electrical panels, stabilizers and solar solutions.', icon: <Zap /> }, { title: 'Transparent starting point', text: 'Preliminary estimates are clearly marked and final recommendations follow assessment.', icon: <Check /> }] }
const panelPage = { kicker: 'ELECTRICAL PANEL MANUFACTURING', title: 'Designed around your power distribution needs.', intro: 'Electrical panels form the backbone of a safe and organized power distribution system. Configurations can be shaped around application and project requirements.', overline: 'PANEL CATEGORIES', heading: 'Practical panel options, subject to confirmed project scope.', description: 'The categories below are structured as editable data. Confirm the actual manufacturing range, specifications and standards before publishing them as a final product list.', bandTitle: 'Clear distribution starts with the requirement.', bandText: 'Share the application, connected load and electrical arrangement so the panel discussion starts with useful context.', cards: panelTypes.map((item) => ({ title: item.title, text: item.text, icon: <Factory />, href: '/contact' })) }
const stabilizerPage = { kicker: 'STABILIZER SOLUTIONS', title: 'Stable voltage. Protected equipment.', intro: 'Voltage fluctuations can affect the performance and life of electrical and electronic equipment. Start with the application and equipment requirement.', overline: 'EQUIPMENT PROTECTION', heading: 'Stabilizer solutions for different applications.', description: 'Capacity ranges and supported brands are deliberately marked as placeholders until the company confirms its current product range.', bandTitle: 'Protection should match the equipment.', bandText: 'A proper recommendation considers the connected equipment, supply conditions, phase arrangement and operating environment.', cards: stabilizerTypes.map((item) => ({ title: item.title, text: `${item.text} ${item.detail}`, icon: <ShieldCheck />, href: '/contact' })) }
const servicesPage = { kicker: 'SERVICES & MAINTENANCE', title: 'More than supply. We support your power systems.', intro: 'From installation and commissioning to repair, preventive maintenance and AMC, our service approach continues beyond the initial supply.', overline: 'SUPPORT THAT CONTINUES', heading: 'A practical service relationship for important equipment.', description: 'Service coverage depends on the equipment, site and requirement. Emergency availability and response times should be added only after they are confirmed by the company.', bandTitle: 'Reliable equipment needs reliable attention.', bandText: 'Tell us what equipment you have, what issue you are facing and where the site is located so the team can understand the next step.', cards: services.map((item) => ({ title: item.title, text: item.text, icon: <Wrench />, href: '/contact' })) }
const contactPage = { kicker: 'CONTACT & GET A QUOTE', title: 'Let us understand your power requirement.', intro: 'Tell us whether you need solar, generator backup, electrical panels, stabilizers or service support.', overline: 'START A CONVERSATION', heading: 'A few details help us prepare the right next step.', description: 'Phone numbers and email addresses are still placeholders in this preview. Replace them in src/data/config.ts before publishing the production site.', bandTitle: 'Prefer a direct conversation?', bandText: `Visit us at ${company.address}. You can also update the phone, email and WhatsApp fields in the central configuration file.`, cards: [{ title: 'Solar consultation', text: 'Share your monthly bill, property type and approximate roof area.', icon: <Sun />, href: '#quote' }, { title: 'Generator requirement', text: 'Share your application, estimated load or equipment list.', icon: <BatteryCharging />, href: '#quote' }, { title: 'Service request', text: 'Tell us the equipment type, issue and preferred callback time.', icon: <Wrench />, href: '#quote' }, { title: 'Visit our location', text: company.address, icon: <Factory />, href: '#quote' }] }

function Footer() { return <footer className="footer"><div className="container footer-grid"><div className="footer-company"><a href="/" className="brand"><span className="brand-mark"><Zap size={18} fill="currentColor" /></span><span><strong>shriamman</strong><small>ENTERPRISES</small></span></a><p>Shri Amman Enterprises provides practical power infrastructure for homes, businesses and industrial operations in Chennai.</p><p className="footer-muted">Generator sales and service, electrical panels, stabilizers, solar planning, installation support and AMC.</p></div><div><p className="footer-label">QUICK LINKS</p><a href="/solar-solutions">Solar solutions</a><a href="/generator-solutions">Generator solutions</a><a href="/projects">Projects &amp; applications</a><a href="/about">About the company</a><a href="/contact">Contact and quote</a></div><div><p className="footer-label">SERVICES</p><a href="/generator-solutions">Generator sales &amp; service</a><a href="/generator-solutions">Electrical panels</a><a href="/generator-solutions">Stabilizers</a><a href="/solar-solutions">Solar energy solutions</a><a href="/generator-solutions">AMC and maintenance</a></div><div><p className="footer-label">CONTACT</p><p>{company.address}</p><p>Phone: {company.phone}<br />Email: {company.email}<br />WhatsApp: {company.whatsapp}</p></div><div className="footer-business"><p className="footer-label">BUSINESS DETAILS</p><p><strong>Established {company.established}</strong><br />{new Date().getFullYear() - company.established} years of experience</p><p>GST IN: {company.gst}</p><p>Serving Chennai and surrounding requirements.</p></div></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} {company.name}. All rights reserved.</span><span>Estimates are preliminary and subject to site assessment.</span></div></footer> }

function Result({ label, value }: { label: string; value: string }) { return <div className="result-card"><span>{label}</span><strong>{value.replace('Not configured years', 'Not configured')}</strong></div> }
function Service({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <article className="service-card"><span className="service-icon">{icon}</span><h3>{title}</h3><p>{text}</p><ArrowRight size={18} /></article> }
function MatrixCard({ icon, number, title, text, href }: { icon: React.ReactNode; number: string; title: string; text: string; href: string }) { return <a className="matrix-card" href={href}><span className="service-icon">{icon}</span><p className="card-label">{number} / POWER SOLUTION</p><h3>{title}</h3><p>{text}</p><strong>Explore <ArrowRight size={15} /></strong></a> }
function ReviewPlaceholder() { return <article className="review-card"><div className="review-stars">★★★★★</div><p>[ADD VERIFIED CUSTOMER REVIEW]</p><footer><strong>[CUSTOMER NAME]</strong><span>[APPLICATION / LOCATION]</span></footer></article> }

export default App
